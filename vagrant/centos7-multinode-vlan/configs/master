# Configuration based on section 1.2 
# url: https://github.com/cloudbase/salt-openstack
#
# Note: /srv/pillar and /srv/salt are mapped by Vagrant to the vm
# as a shared folder

pillar_roots:
  openstack:
    - /srv/pillar
file_roots:
  openstack:
    - /srv/salt
jinja_trim_blocks: True
jinja_lstrip_blocks: True
